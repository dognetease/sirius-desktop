import React from 'react';

const defaultStyle = {
  width: '100%',
  height: '44px',
  backgroundColor: 'rgba(246, 246, 249, 1)',
};

const searchBar: React.FC<any> = () => <div style={defaultStyle} />;

export default searchBar;
