import React, { useEffect, useState, useCallback, useRef, useMemo } from 'react';

import styles from './privateSpace.module.scss';

const privateSpace: React.FC<any> = ({}) => <div className={styles.privateSpace}>11</div>;

export default privateSpace;
